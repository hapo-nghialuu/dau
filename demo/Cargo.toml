[package]
name = "dau-demo"
version = "0.1.0"
edition = "2021"
description = "WASM demo wrapper cho dau-core (chạy thử engine trên trình duyệt/macOS)"

[lib]
crate-type = ["cdylib", "staticlib"]

[dependencies]
dau-core = { path = "../core" }

[profile.release]
opt-level = "s"
lto = true
